'use strict';
var express = require('express')
const app = express();

//Escuchamos nuestro servidor creado
app.listen(3001, () => {
  console.log('listening on 3001')
})


// Primero npm init = iniciar el proyecto -> package.json

// Segundo crear index.js

// Tercero Instalar todas las librerias que contiene nuestro package.json  (npm install)

// Cuarto Instalar express (npm install express)

// Finaliza

//Prueba de git ignore